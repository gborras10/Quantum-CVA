Deprecated compatibility launchers for the reusable AE experiment package.

The old experiment scripts in this tree now delegate to
`quantum_cva.amplitude_estimation.experiments`. Duplicated utilities were
intentionally not restored.

The selected `experiment_results` directories retained here are historical
experiment outputs kept as curated reference results for this branch.

